import 'package:flutter/material.dart';

class PrepStage {
  final title, procedure, duration, image;
  final int stageNo;

  PrepStage(
      {@required this.title,
      @required this.procedure,
      @required this.duration,
      @required this.image,
      @required this.stageNo});
}
