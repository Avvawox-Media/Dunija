import 'package:flutter/material.dart';

class StageData {
  final title, description, duration, image;

  StageData(
      {@required this.title,
      @required this.description,
      @required this.duration,
      @required this.image});
}
